# Supabase notes

This directory is reserved for future Supabase files such as:

- SQL migrations
- seed data
- Edge Functions
- local CLI config

Recommended next step:

```bash
npx supabase init
```

After that, you can add migrations here and keep your database changes under version control.
